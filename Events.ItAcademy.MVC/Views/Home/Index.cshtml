@*@{
    ViewData["Title"] = "Home Page";
}
@model List<Events.ItAcademy.Application.Events.Responses.EventResponseModel>


<h1>EventsDetails</h1>
@foreach (var item in Model)
{
    <div>
        <h2>@item.Title</h2>
        <p>@item.Description</p>
        <p>@item.Price</p>
        <p>@item.TicketCount</p>
        <p>@item.StartDate</p>
        <p>@item.EndDate</p>
    </div>
}
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>
*@

@model List<Events.ItAcademy.Application.Events.Responses.EventsWithoutDetails>



@{
    ViewData["Title"] = "Home Page";
    foreach (var item in Model)
    {
        item.PhotoPath = "~/images/" + (item.PhotoPath ?? "noimage.jpg");
    }
}
<h1>Events</h1>

@foreach (var item in Model)
{
    <div style="margin-bottom:20px;">
        <div class="card gx-md-3" style="border: 3px solid #ccc; justify-content:space-between; display:flex; padding: 10px; margin-bottom: 3px;">
            <div class="row">
                <div class="col-md-6">
                    <h2>Title: @item.Title</h2>
                    <p>Price: @item.Price</p>
                </div>
                <div class="col-md-6">
                    <img class="w-100 h-100 object-cover imageThumbnail" style="border-radius:20px" src="@item.PhotoPath" asp-append-version="true" />
                </div>
            </div>
        </div>

        <a asp-controller="Events" asp-action="ViewDetails" asp-route-eventId="@item.Id">View Details</a>
    </div>
}



